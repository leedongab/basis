+형변환
자동형변환과 명시적 형 변환이 있으며,자동 형변환은 자동으로 바꿔주고
명시적은 ()를 통해 데이터 타입을 바꿔 준다.
ex)	float a = 10.0;
	int b = 10.0F; //자동형변환


	int b = (int)10.0F; //명시적 형 변환 float 형을  정수로 형변환!

	나중에는 object 등을  String 같은 것으로 형변환 하는 경우가 종종 생긴다.