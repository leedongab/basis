+java 및 다른 프로그래밍 언어는 자료형이 있다.

1.int - 숫자 즉 연산이 가능한 자료형이다.
2.char - 글자 하나에 대한 자료형이다.
3.String - char를 모아서 만든 객체 자료형이다.
4.boolean - 논리형 true,false로 나뉜다. 50대50으로 표현 가능하다.
5.float - 소수점으로 나타내는 숫자 에 맞는 실수형 4byte
6.double -  소수점으로 나타내는 숫자 에 맞는 실수형 8byte
--------------------------------------------------------------------------------------------------------------------------------------------
+ java 연산자
+,-,=,*,==,!= 등 다양한 연산자가 있다
헷갈리는 부분 은 = 흔히 일상적으로 쓰이는 같다라는 의미가 아니라  대입연산자로 쓰이며,
흔히 왼쪽은 [변수] [대입연산자] [데이터값] ; 이런식으로 쓰인다.
--------------------------------------------------------------------------------------------------------------------------------------------
+java 변수

데이터의 흐름에 따라 변할 수 있는 수
전역변수  : class 안에서 계속 존재한다.(인스턴스 변수라도 불림)
지역변수 : {} 안에서 선언 및 초기화 해야하며  해당{} 안에서만 활동하는 변수
매개변수 : 값을 전달 해주는 변수이며 보통 메소드 안에 () 를 통해  참조 데이터 타입인지 가변적 매개변수로 나뉜다.
--------------------------------------------------------------------------------------------------------------------------------------------
+ java 접근제한자

클래스나 변수에 대한 접근권한을 주는 것

protected : 모든 클래스에서의 권한만 주지 않는다. /동일 패키지와 동일 패키지에 포함되지는 않지만 하위 클래스 관계에 있는 클래스도 접근할 수 있다.
public : 모든 클래스에서 권한을 준다. /모든 클래스에 의해 접근할 수 있다.  즉, 접근 제약이 없다.
private: 해당 클래스에서만 사용 가능하게 권한을 준다. //자기 자신 클래스 내에서만 이용
--------------------------------------------------------------------------------------------------------------------------------------------





